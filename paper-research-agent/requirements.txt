# 핵심 — 이 넷만 있으면 전부 동작합니다.
requests>=2.31.0
PyYAML>=6.0

# PDF 처리
pdfplumber>=0.11.0       # 1순위 추출기
pypdf>=5.0.0             # 폴백 추출기

# 개발용
pytest>=8.0.0

# --- 선택 사항 ---------------------------------------------------------------
# OpenAI / Anthropic / Gemini / Ollama 는 별도 설치 없이 바로 됩니다.
# 그 외 공급자(Bedrock, Azure, Vertex 등)를 쓰실 때만 아래를 설치하세요.
# litellm 은 pydantic 등 13개 패키지를 끌고 오고, 그중 하나라도 설치가
# 깨지면 프로그램 전체가 안 뜹니다. 필요할 때만 넣는 걸 권합니다.
#
#   pip install litellm
